   // onTap: () async {

                      // final reachBottom = !isOnTop;
                      // final max = _scrollController.position.maxScrollExtent;
                      // final current = _scrollController.position.pixels;
                      // debugPrint(" current: ${current} max: ${max}");

                      // if (max - current > 3000) {
                      //   debugPrint(" Toggling");
                      //   setState(() {
                      //     scrollDirection = !scrollDirection;
                      //     isOnTop = !isOnTop;
                      //     widget.data = widget.data.copyWith(
                      //         messages:
                      //             widget.data.messages.reversed.toList());
                      //   });
                      //   await Future.delayed(
                      //       const Duration(milliseconds: 300));

                      //   double scrollAmount = scrollDirection && isOnTop
                      //       ? _scrollController.position.maxScrollExtent
                      //       : 0;
                      //   _scrollController.jumpTo(scrollAmount);
                      //   final max =
                      //       _scrollController.position.maxScrollExtent;
                      //   final current = _scrollController.position.pixels;
                      //   debugPrint(" current: ${current} max: ${max}");
                      // } else {
                      //   double scrollAmount = scrollDirection && !isOnTop
                      //       ? _scrollController.position.maxScrollExtent
                      //       : 0;
                      //   _scrollController.jumpTo(scrollAmount);
                      //   setState(() {
                      //     isOnTop = !isOnTop;
                      //   });
                      // }
                      // },


  // child: ListView.builder(
                //   reverse: scrollDirection,
                //   controller: _scrollController,
                //   itemCount: widget.data.messages.length,
                //   itemBuilder: (context, index) {
                //     bool isMe = widget.data.messages[index].name ==
                //         widget.data.names[direction];
                //     return Row(
                //       mainAxisAlignment: isMe
                //           ? MainAxisAlignment.end
                //           : MainAxisAlignment.start,
                //       children: [
                //         ChatBubble(
                //           widget.data.messages[index],
                //           isMe,
                //           dir: widget.data.chatDir,
                //         ),
                //       ],
                //     );
                //   },
                // ),